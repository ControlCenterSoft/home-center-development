{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://home-center.local/contracts/devices/device-management-enrollment-verification-confirm-request.v1.schema.json",
  "title": "Home Center device management enrollment verification confirm request v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema", "verification_id", "confirmed", "idempotency_key"],
  "properties": {
    "schema": {"const": "home-center.device-management-enrollment-verification-confirm-request.v1"},
    "verification_id": {"type": "string", "pattern": "^dmpverify-[0-9a-f]{24}$"},
    "confirmed": {"const": true},
    "idempotency_key": {"type": "string", "minLength": 1, "maxLength": 128, "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"}
  }
}
