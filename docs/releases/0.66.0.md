# Home Center 0.66.0

Status: source preparation only; not Release Candidate and not Public Stable.

Dependency boundary: this source slice is stacked on the 0.65 ZigBee/MQTT plan-only foundation. It must be transplanted/reconciled onto the exact canonical 0.65 ancestry before qualification; no 0.65 or 0.66 Stable claim is made here.

## Scope

0.66 prepares the Smart Home device lifecycle/read-model layer for «Мой дом»:

- exact binding to freshness-bound 0.65 inventory evidence and local Home Center profile generation;
- deterministic capability-family mapping for Cozy UX;
- truthful health states `healthy/degraded/stale/unknown`;
- bounded diagnostics for low battery, weak link, stale/unavailable evidence and room drift;
- room drift only from current/current evidence;
- separate Cozy projection without provider/coordinator transport details and Full technical projection with exact evidence;
- exact-bound plan-only rename and move-room operations;
- mandatory explicit confirmation and recovery snapshot for future change execution;
- stale/no-op/cross-operation field smuggling rejected fail-closed.

## Safety boundary

This source does not perform device mutation. `execution_authorized`, `provider_execution_authorized`, `device_state_mutation_authorized`, `infrastructure_mutation_authorized` and `external_publication_authorized` remain false.

Pair/unpair continue to use the separate 0.65 operation boundary. A normal rename/move flow cannot be used as an alternate pairing, provider, infrastructure or external-publication path. Physical single-coordinator HA limitations from 0.65 remain unchanged and no automatic failover is claimed.

## Remaining work before any release claim

0.66 still requires canonical persistence/profile migration design, authenticated Household/RBAC API, durable Change/Job execution, optimistic generation CAS, Audit/recovery evidence, post-condition read-back, actual Cozy/Full route integration, install/upgrade/rollback qualification, security/privacy checks, reproducible packaging and exact release identity. Commercial-launch/legal clearance remains a separate claim boundary and is not inferred by this source preparation.
