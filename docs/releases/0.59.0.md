# Home Center 0.59.0

Status: DEVELOPMENT WORK BRANCH / NOT RELEASE CANDIDATE

Public Stable baseline: 0.56.0.

Stacked development base for this work: Home Center 0.58 branch identity `44bb2c60c755c8016beada228eaaafed852f33fa`.

## Planned release scope

Home Center 0.59 introduces Policy Composer v1. RolePreset and PolicyBundle become an exact-state planning boundary for protected Desired State. The parent-facing «Уютный» representation explains the effective result in household language while the Full/Core representation remains deterministic and technically exact.

## Prepared implementation

The current work branch contains:

- deterministic `PolicyBundle` identity;
- RolePreset safety ceiling that permits tightening but rejects privilege broadening;
- exact Household snapshot/resource-version/generation binding;
- parent-only policy planning boundary;
- deterministic `PolicyChangePlan` evidence;
- explicit confirmation after exact-state revalidation;
- deterministic confirmation/audit identities;
- plain-language Russian summary for Cozy UI;
- recovery binding to the previous bundle without automatic rollback authority;
- JSON Schema contracts for bundle, plan and confirmation;
- negative tests for child-role privilege broadening, stale-state rejection, actor substitution and forged bundle identity.

## Safety boundary

The prepared 0.59 slice does not authorize:

- provider execution;
- infrastructure or network mutation;
- external publication;
- automatic recovery;
- 0.60 Internet/DNS/Proxy enforcement capabilities.

Confirmation authorizes only a future protected Desired State write after the durable API/RBAC/Audit integration is added.

## Remaining before qualification

- durable protected Desired State persistence;
- API integration through Identity/RBAC, server-side actor/member binding and re-auth;
- idempotency and durable Job/Audit evidence;
- recovery/read-back receipts for Desired State transition;
- shared Cozy/Full representation of the same policy state;
- backup/restore and upgrade preservation of policy state;
- complete schema/runtime negative tests;
- exact-head qualification and release gates;
- commercial/security review applicable to the final 0.59 scope.

No Release Candidate or Public Stable claim is made by this document.
