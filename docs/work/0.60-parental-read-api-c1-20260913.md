# Home Center 0.60 — parental read/preview C1 handoff

Status: **NON-RUNNER SOURCE PREPARED / NOT QUALIFIED**.

Base: canonical `main` `4cdd7967ce315482ed4f4f2862f1a092b974014e` (Public Stable baseline 0.59.0).

Branch: `nr1/0.60-parental-read-api-c1-20260913`.

## Prepared source delta

- authenticated `RuntimeRequestHandlerV9` layered on V8;
- parent-authorized saved parental Desired State GET;
- same-origin, side-effect-free decision-preview POST;
- server-side policy/rule-source identity so callers cannot select policy identity or authorization material;
- exact current verified-0.59 base revalidation before previews;
- Cozy/Full saved parental policy visibility in the existing family-policy UI;
- closed JSON contracts and OpenAPI boundary;
- release wheel membership for `api_v9.py` and `parental_internet_policy_read_api.py`;
- source tests for contracts, route composition, exact-state/fail-closed behavior and UI privacy.

## Safety boundary

This slice deliberately does **not** expose parental policy plan/confirm, scoped re-auth, DNS/proxy apply, provider execution, rollback, infrastructure mutation or external publication. Decision previews are explicitly marked `preview_only=true`, `enforcement_verified=false` and `provider_execution_authorized=false`.

## Runner handoff

No workflow/check was started while preparing this branch. A later runner-enabled flow may qualify the exact head once capacity policy permits. Required useful qualification is one exact-head normal Home Center CI/privacy/infrastructure-neutrality pass plus the existing reproducible artifact checks; do not start duplicate/rerun work if another flow is already qualifying the same SHA.
