# Home Center

Home Center is an infrastructure-neutral platform for managing home and small-server infrastructure through a unified Web UI and API.

This branch develops portable infrastructure inventory normalization. Node identity, topology and addressing are runtime inputs and are never bound to a specific deployment.
