# Home Center

This branch develops the local-first infrastructure-neutral Device Registry. RFC3339 timestamps with explicit offsets are normalized to UTC. Device identifiers, locations, ownership and permissions are runtime data.
