# Home Center

Home Center is an infrastructure-neutral platform for managing home and small-server infrastructure through a unified Web UI and API.

This branch develops read-only node discovery from trusted inventory. Discovery accepts runtime observations and never assumes fixed node names, addresses, domains or topology.
